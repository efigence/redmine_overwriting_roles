<h2><%= @role.name %></h2>

<%= labelled_form_for @project_role, :url => save_project_role_path(@project, @role) do |f| %>
<%= render :partial => 'form', :locals => { :f => f } %>
<%= submit_tag l(:button_save) %>
<% end %>
